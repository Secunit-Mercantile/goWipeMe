name: Release

on:
  push:
    tags:
      - 'v*.*.*'  # Triggers on version tags like v1.0.0

permissions:
  contents: write

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Run tests
        run: go test -v ./internal/...

  build-tui:
    name: Build TUI
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: darwin
            goarch: amd64
            name: macos-intel
          - goos: darwin
            goarch: arm64
            name: macos-apple-silicon
          - goos: linux
            goarch: amd64
            name: linux-amd64
          - goos: linux
            goarch: arm64
            name: linux-arm64
          - goos: linux
            goarch: riscv64
            name: linux-riscv64
          - goos: windows
            goarch: amd64
            name: windows-amd64
            ext: .exe
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Build TUI
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
        run: |
          go build -ldflags="-s -w" -trimpath -o gowipeme${{ matrix.ext }} ./cmd/gowipeme

      - name: Compress binary (Unix)
        if: matrix.goos != 'windows'
        run: |
          tar -czf gowipeme-tui-${{ matrix.name }}.tar.gz gowipeme

      - name: Compress binary (Windows)
        if: matrix.goos == 'windows'
        run: |
          zip gowipeme-tui-${{ matrix.name }}.zip gowipeme.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: tui-${{ matrix.name }}
          path: gowipeme-tui-${{ matrix.name }}.*

  build-gui-macos:
    name: Build GUI (macOS)
    needs: test
    runs-on: macos-latest
    strategy:
      matrix:
        include:
          - platform: darwin/universal
            name: macos-universal
          - platform: darwin/amd64
            name: macos-intel
          - platform: darwin/arm64
            name: macos-apple-silicon
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Bun
        run: |
          curl -fsSL https://bun.sh/install | bash
          echo "$HOME/.bun/bin" >> $GITHUB_PATH

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Install frontend dependencies
        run: cd frontend && bun install

      - name: Build GUI
        run: |
          wails build -clean -platform ${{ matrix.platform }} -ldflags="-s -w" -trimpath -o gowipeme-gui

      - name: Compress app bundle
        run: |
          cd build/bin
          tar -czf ../../gowipeme-gui-${{ matrix.name }}.tar.gz goWipeMe.app

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: gui-${{ matrix.name }}
          path: gowipeme-gui-${{ matrix.name }}.tar.gz

  build-gui-linux:
    name: Build GUI (Linux)
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - platform: linux/amd64
            name: linux-amd64
          - platform: linux/arm64
            name: linux-arm64
          - platform: linux/riscv64
            name: linux-riscv64
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: |
          # For ARM64, we need cross-compilation toolchain and ARM64 libraries
          if [ "${{ matrix.platform }}" = "linux/arm64" ]; then
            # Enable multiarch for ARM64 FIRST, then update
            sudo dpkg --add-architecture arm64
            sudo apt-get update
            # Install ARM64 cross-compilation toolchain
            sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
            # Check available ARM64 packages
            echo "Checking available ARM64 packages..."
            apt-cache search libgtk-3-dev:arm64 || apt-cache search libgtk-3 | grep arm64 || true
            apt-cache search webkit2gtk | grep arm64 || true
            # Try to install ARM64 libraries - use available versions
            sudo apt-get install -y libgtk-3-dev:arm64 || echo "libgtk-3-dev:arm64 not available, trying alternative..."
            sudo apt-get install -y libwebkit2gtk-4.1-dev:arm64 || sudo apt-get install -y libwebkit2gtk-4.0-dev:arm64 || echo "webkit2gtk dev package not available for arm64"
            # Create symlink for ARM64 if needed
            if [ -f /usr/lib/aarch64-linux-gnu/pkgconfig/webkit2gtk-4.1.pc ] && [ ! -f /usr/lib/aarch64-linux-gnu/pkgconfig/webkit2gtk-4.0.pc ]; then
              sudo ln -s /usr/lib/aarch64-linux-gnu/pkgconfig/webkit2gtk-4.1.pc /usr/lib/aarch64-linux-gnu/pkgconfig/webkit2gtk-4.0.pc
              echo "✓ Created webkit2gtk-4.0 symlink for ARM64"
            fi
            # Verify pkg-config setup
            export PKG_CONFIG_PATH=/usr/lib/aarch64-linux-gnu/pkgconfig:$PKG_CONFIG_PATH
            aarch64-linux-gnu-pkg-config --exists webkit2gtk-4.0 && echo "✓ webkit2gtk-4.0 found for ARM64" || echo "⚠ webkit2gtk-4.0 not found for ARM64"
          else
            # For AMD64 and RISC-V, use native libraries
            sudo apt-get update
            sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev
            # Wails looks for webkit2gtk-4.0, but Ubuntu has 4.1 - create symlink
            if [ -f /usr/lib/x86_64-linux-gnu/pkgconfig/webkit2gtk-4.1.pc ] && [ ! -f /usr/lib/x86_64-linux-gnu/pkgconfig/webkit2gtk-4.0.pc ]; then
              sudo ln -s /usr/lib/x86_64-linux-gnu/pkgconfig/webkit2gtk-4.1.pc /usr/lib/x86_64-linux-gnu/pkgconfig/webkit2gtk-4.0.pc
            fi
            pkg-config --exists webkit2gtk-4.0 && echo "✓ webkit2gtk-4.0 found" || echo "⚠ webkit2gtk-4.0 not found"
          fi

      - name: Install Bun
        run: |
          curl -fsSL https://bun.sh/install | bash
          echo "$HOME/.bun/bin" >> $GITHUB_PATH

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Install frontend dependencies
        run: cd frontend && bun install

      - name: Build GUI
        env:
          CC: ${{ matrix.platform == 'linux/arm64' && 'aarch64-linux-gnu-gcc' || '' }}
          CXX: ${{ matrix.platform == 'linux/arm64' && 'aarch64-linux-gnu-g++' || '' }}
          PKG_CONFIG: ${{ matrix.platform == 'linux/arm64' && 'aarch64-linux-gnu-pkg-config' || '' }}
          PKG_CONFIG_PATH: ${{ matrix.platform == 'linux/arm64' && '/usr/lib/aarch64-linux-gnu/pkgconfig' || '' }}
          CGO_ENABLED: 1
          GOOS: ${{ matrix.platform == 'linux/arm64' && 'linux' || '' }}
          GOARCH: ${{ matrix.platform == 'linux/arm64' && 'arm64' || '' }}
        run: |
          wails build -clean -platform ${{ matrix.platform }} -ldflags="-s -w" -trimpath -o gowipeme-gui

      - name: Check build output
        id: check_build
        continue-on-error: true
        run: |
          if [ -d "build/bin" ] && [ -f "build/bin/gowipeme-gui" ]; then
            echo "build_success=true" >> $GITHUB_OUTPUT
            echo "✓ Build successful, binary found"
          else
            echo "build_success=false" >> $GITHUB_OUTPUT
            echo "✗ Build directory or binary not found"
            echo "Build directory structure:"
            ls -la build/ 2>/dev/null || echo "build/ directory does not exist"
            echo ""
            echo "Searching for gowipeme-gui binary:"
            find . -name "gowipeme-gui" -type f 2>/dev/null || echo "gowipeme-gui binary not found"
          fi

      - name: Compress binary
        if: steps.check_build.outputs.build_success == 'true'
        run: |
          cd build/bin
          tar -czf ../../gowipeme-gui-${{ matrix.name }}.tar.gz gowipeme-gui
          echo "✓ Binary compressed successfully"

      - name: Skip if build not supported
        if: steps.check_build.outputs.build_success != 'true' && matrix.platform == 'linux/riscv64'
        run: |
          echo "⚠ WARNING: RISC-V 64 may not be supported by Wails for GUI builds"
          echo "Skipping this build target"
          echo "This is expected if Wails does not support RISC-V 64 architecture"

      - name: Fail if build not found (non-RISC-V)
        if: steps.check_build.outputs.build_success != 'true' && matrix.platform != 'linux/riscv64'
        run: |
          echo "ERROR: Build failed or binary not found for ${{ matrix.platform }}"
          exit 1

      - name: Upload artifact
        if: steps.check_build.outputs.build_success == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: gui-${{ matrix.name }}
          path: gowipeme-gui-${{ matrix.name }}.tar.gz

  build-gui-windows:
    name: Build GUI (Windows)
    needs: test
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Bun
        run: |
          powershell -c "irm bun.sh/install.ps1 | iex"
          echo "$env:USERPROFILE\.bun\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Install frontend dependencies
        run: cd frontend && bun install

      - name: Build GUI
        run: |
          wails build -clean -platform windows/amd64 -ldflags="-s -w" -trimpath -o gowipeme-gui.exe

      - name: Compress binary
        run: |
          cd build/bin
          Compress-Archive -Path gowipeme-gui.exe -DestinationPath ../../gowipeme-gui-windows-amd64.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: gui-windows-amd64
          path: gowipeme-gui-windows-amd64.zip

  create-release:
    name: Create Release
    needs: [build-tui, build-gui-macos, build-gui-linux, build-gui-windows]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: Display structure of downloaded files
        run: ls -R ./artifacts

      - name: Prepare release assets
        run: |
          mkdir -p release
          find ./artifacts -type f \( -name "*.tar.gz" -o -name "*.zip" \) -exec cp {} ./release/ \;

      - name: Generate release notes
        id: release_notes
        run: |
          cat > release_notes.md << 'EOF'
          ## goWipeMe ${{ github.ref_name }}

          Privacy tool for macOS, Linux, and Windows.

          ### What's New in ${{ github.ref_name }}
          
          - 📸 Added application screenshot to README
          - 🎨 Added transparent logo to README
          - 🔧 Improved GitHub Actions workflow for automatic release creation

          ### Features
          - 🗑️ **Backup** - Save browser & shell history before cleaning
          - 🔄 **Restore** - Restore from previous backups
          - 🧹 **Clear All History** - Remove browser history, shell history, caches, recent files, clipboard
          - 🔒 **Secure Wipe Free Space** - DoD-grade disk wiping (Single Pass, DoD 5220.22-M, Gutmann)

          ### Downloads

          #### TUI (Terminal Interface)
          - **macOS Intel**: `gowipeme-tui-macos-intel.tar.gz`
          - **macOS Apple Silicon**: `gowipeme-tui-macos-apple-silicon.tar.gz`
          - **Linux AMD64**: `gowipeme-tui-linux-amd64.tar.gz`
          - **Linux ARM64**: `gowipeme-tui-linux-arm64.tar.gz`
          - **Linux RISC-V 64**: `gowipeme-tui-linux-riscv64.tar.gz`
          - **Windows**: `gowipeme-tui-windows-amd64.zip`

          #### GUI (Graphical Interface)
          - **macOS Universal** (Intel + Apple Silicon): `gowipeme-gui-macos-universal.tar.gz`
          - **macOS Intel**: `gowipeme-gui-macos-intel.tar.gz`
          - **macOS Apple Silicon**: `gowipeme-gui-macos-apple-silicon.tar.gz`
          - **Linux AMD64**: `gowipeme-gui-linux-amd64.tar.gz`
          - **Linux ARM64**: `gowipeme-gui-linux-arm64.tar.gz`
          - **Linux RISC-V 64**: `gowipeme-gui-linux-riscv64.tar.gz`
          - **Windows**: `gowipeme-gui-windows-amd64.zip`

          ### Installation

          **TUI:**
          ```bash
          tar -xzf gowipeme-tui-*.tar.gz
          ./gowipeme
          ```

          **GUI (macOS):**
          ```bash
          tar -xzf gowipeme-gui-macos-*.tar.gz
          open goWipeMe.app
          ```

          **GUI (Linux):**
          ```bash
          tar -xzf gowipeme-gui-linux-*.tar.gz
          ./gowipeme-gui
          ```

          **GUI (Windows):**
          ```
          Extract gowipeme-gui-windows-amd64.zip
          Run gowipeme-gui.exe
          ```

          ### Safety Features
          - Two-phase disk wiping prevents OS crashes
          - Backups stored locally in `~/.gowipeme/backups/`
          - All operations are reversible via restore

          ---

          **⚠️ Warning:** All cleaning operations are permanent. Create a backup first!
          EOF
          echo "notes_file=release_notes.md" >> $GITHUB_OUTPUT

      - name: Extract tag name
        id: tag
        run: |
          TAG_NAME=${GITHUB_REF#refs/tags/}
          echo "tag_name=$TAG_NAME" >> $GITHUB_OUTPUT
          echo "Creating release for tag: $TAG_NAME"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.tag.outputs.tag_name }}
          name: Release ${{ steps.tag.outputs.tag_name }}
          files: release/*
          body_path: ${{ steps.release_notes.outputs.notes_file }}
          draft: false
          prerelease: false
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
